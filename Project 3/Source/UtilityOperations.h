#ifndef UTILITY_OPERATIONS
#define UTILITY_OPERATIONS

#include <vector>

int minimumDistance(std::vector<bool> visitedArray, std::vector<int> distances, std::vector<std::vector<int>> associativeMatrix);
bool validateCycle(std::vector<std::vector<int>> cycless);

#endif